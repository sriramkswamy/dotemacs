;;; sk-org-hydra-bindings-which-key.el --- Global settings -*- lexical-binding: t; -*-

;;; Commentary:

;; which key explanations for org hydra bindings

;;; Code:

(which-key-add-key-based-replacements
  "C-c h o" "org hydra prefix")

(provide 'sk-org-hydra-bindings-which-key)
;;; sk-org-hydra-bindings-which-key.el ends here
