;;; sk-editing-functions-bindings-which-key.el --- Global settings -*- lexical-binding: t; -*-

;;; Commentary:

;; which key explanations for bindings in sk-editing-functions-bindings.el

;;; Code:

(which-key-add-key-based-replacements
  "C-c m" "move prefix")

(provide 'sk-editing-functions-bindings-which-key)
;;; sk-editing-functions-bindings-which-key.el ends here
