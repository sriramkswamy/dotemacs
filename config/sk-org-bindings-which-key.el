;;; sk-org-bindings-which-key.el --- Global settings -*- lexical-binding: t; -*-

;;; Commentary:

;; Org mode bindings

;;; Code:

(which-key-add-key-based-replacements
  "C-c O" "org mode prefix")

(provide 'sk-org-bindings-which-key)
;;; sk-org-bindings-which-key.el ends here
