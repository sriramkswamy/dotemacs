;;; sk-visual-hydra-bindings-which-key.el --- Global settings -*- lexical-binding: t; -*-

;;; Commentary:

;; which key for bindings of visual hydras

;;; Code:

(which-key-add-key-based-replacements
  "C-c v z" "fold prefix")

(provide 'sk-visual-hydra-bindings-which-key)
;;; sk-visual-hydra-bindings-which-key.el ends here
